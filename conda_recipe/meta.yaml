package:
  name: cgal4py
  version: "0.2.0"

source:
  # path: ../
  fn: cgal4py-0.2.0.tar.gz
  url: https://pypi.python.org/packages/2b/e8/3d8dd74d29e373b2bb915f5615edd1de91250d53b0e4c2919449d39b8bcf/cgal4py-0.2.0.tar.gz
  md5: e54b67d4e041069ff6df5ab65584aa18
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  script:
    - flake8 . -v --config=.flake8rc
    - nosetests --verbose
    - python setup.py install
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - cgal4py = cgal4py:main
    #
    # Would create an entry point called cgal4py that calls cgal4py.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - nose
    - flake8
    - cython
    - numpy
    - cykdtree
    - boost
    - eigen3
    - cgal >=4.9

  run:
    - python
    - cython
    - numpy
    - scipy
    - matplotlib
    - nose
    - mpi4py
    - cykdtree
    - boost
    - eigen3
    - cgal >=4.9
    # Python 2 only
    - multiprocessing

test:
  # Python imports
  imports:
    - cgal4py
    - cgal4py.delaunay
    - cgal4py.domain_decomp
    - cgal4py.tests

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    - nose

about:
  home: https://langmm@bitbucket.org/langmm/cgal4py
  license: BSD License
  summary: 'Python interface for CGAL Triangulations'
  license_family: BSD

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
